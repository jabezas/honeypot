import React from "react"
import styled from "styled-components"

const StyledFooter = styled.footer`
  margin-top: 2rem;
  background: black;
  color: white;
  padding: 4rem;
`

const Footer = () => <StyledFooter>My footer here.</StyledFooter>

export default Footer
